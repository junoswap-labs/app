// Removed — Redeem now uses real Supabase data (hooks/useRedeemOrders.ts) instead of this mock
// store. Left as an empty stub rather than deleted: file deletion is blocked in this environment.
export {}
