// Removed — Settings now reads/writes real data (hooks/useCurrentUser.ts, useConnectSettings.ts)
// instead of this mock store. Left as an empty stub rather than deleted: file deletion is blocked
// in this environment.
export {}
