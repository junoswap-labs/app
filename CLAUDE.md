# CLAUDE.md

## Project

Junoswap Marketplace — NFT and Real-World-Asset (RWA) trading platform, paid in ERC20 tokens, on Bitkub Chain (kub, chainId 96 mainnet / 25925 testnet) only. Brand/design forked from `junoswap` but a fully separate codebase and deployment (`marketplace.junoswap.trade`). **Never modify the `junoswap` repo** — this repo only copies from it, one-way.

Current active development scope: **Marketplace** (NFT + RWA trading) and **Redeem** (Points + ERC20 → NFT/merch). Adjacent features already scaffolded in this repo — Rebate/staking, Launchpad creator-fee, KYC, admin analytics — share the same stack but aren't part of this scope.

**Marketplace (NFT + RWA) and Redeem are both real, end-to-end** — Supabase-backed data (`supabase/migrations/0001`–`0008`), real wagmi write hooks, a custom `getLogs`-based sync poller as the sole writer of order/escrow status columns, and Route Handlers under `app/api/*` for anything needing auth or a service-role write. See "Clean workflow" below for the exact write pattern. Rebate/staking (`store/mock-rebate.ts`) and KYC are still scaffolded on Zustand mock stores — genuinely out of scope, don't assume they're wired. Launchpad creator-fee is real and tested (`services/marketplace/creator-fee/*`) but is a separate feature, also out of scope here. See `docs/Marketplace_Redeem_Feature.md` for the full current-state reference (gitignored under `/Docs` in `.gitignore`, not committed — local reference only, regenerate/update it by hand as the code changes). Don't assume ABI names, endpoint paths, or Supabase columns exist just because a comment or an older doc references them — verify against actual files first; this file itself has drifted from the code before.

## Directory map

app/                       Next.js App Router — the real product lives under `app/app/*` (URL prefix `/app/*`: `/app`, `/app/nft/list`, `/app/nft/[orderHash]`, `/app/rwa/list`, `/app/rwa/[listingId]`, `/app/orders`, `/app/redeem`, `/app/redeem/[itemId]`, `/app/redeem/list`, `/app/redeem/manage`, `/app/redeem/orders`, `/app/admin`, `/app/settings`). `app/marketplace/*` is a **legacy redirect-only shim** (every page.tsx is `redirect('/app/...')`). `app/page.tsx` redirects to `/app/redeem` (Marketplace nav is temporarily hidden — see `components/marketplace/header.tsx`, routes still work by direct URL). `app/launchpad/creator-fee/` is a separate feature, out of scope here. `app/api/*` is real — Route Handlers for auth (SIWE nonce/verify, Telegram link+webhook), NFT/RWA order + Redeem item/order CRUD, admin queues, image upload (Pinata), and `cron/sync-onchain` + `cron/ship-deadline` + `sync/refresh` for the sync poller. No `.github/workflows/` exists in this repo despite a code comment referencing one — nothing schedules the cron routes today; they must be hit manually or by an external scheduler.
components/marketplace/    Shell — header.tsx only
components/nft/            nft-card, nft-grid, listing-toolbar, buy-nft-dialog (listing happens on the /app/nft/list page directly, no separate list dialog)
components/rwa/            rwa-card, rwa-grid, order-status-tracker, ship-deadline-countdown (dispute UI is inline in these, not a separate dialog; ship-deadline-countdown's `DeadlineCountdown` is shared by Redeem too)
components/redeem/         listed-by, redeem-item-card, redemption-status-tracker, redemption-auto-release (buyer confirm/extend-7-days/claim-timeout panel, reads the RwaEscrow deployment's `autoReleaseExtension` live rather than trusting a DB flag)
components/settings/       telegram-link-card, lister-profile-card ("List By" branding for Registered Redeem partners)
components/admin/          dispute-queue-table, redemption-queue (reused as-is on `/app/redeem/manage` for Registered partners — the API route does the own-listings filtering), kyc-queue, analytics-dashboard
components/kyc/, components/rebate/, components/launchpad/   Adjacent features (out of Marketplace/Redeem scope) — Rebate still runs on `store/mock-rebate.ts`
components/ui/, components/web3/   Forked verbatim (or lightly trimmed) from junoswap — see "Forked from junoswap" below
hooks/                     Real wagmi write hooks exist and follow the Clean Workflow below: useListNftOrder/useCancelNftOrder/useFulfillNftOrder, useCreateRwaListing/useCancelRwaListing/useRwaActions (fund/markShipped/confirmReceived/claimRefund/openDispute/resolveDispute/claimShipmentTimeout), useCreateRedeemItem/useCreateRedeemOrder, useRedeemMerchActions (mirrors useRwaActions against the *second*, Redeem-dedicated RwaEscrow deployment — see contracts/ below), useSyncRefresh (`POST /api/sync/refresh`). Read hooks: useListings/useRwaListings/useRedeemItems (Supabase, public RLS read), useNftMetadata (on-chain), useCurrentUser/useConnectSettings (Telegram), useAnalytics, useListerProfile, useJunoPtsBalance. useClaimCreatorFee/useCreatorFeeClaims are Launchpad, out of scope.
services/marketplace/      Pure logic, no React, all real (not mock) — fee.ts, listing-query.ts, nft-metadata.ts (tokenURI/IPFS resolution), rwa-order.ts (client-side state-machine guard for UX only, mirrors RwaEscrow.sol exactly) · creator-fee/ (Launchpad, real + tested, out of scope). The EIP-712 signing helper lives in `lib/eip712.ts` (both `NftOrder`/`nftOrderDomain` for Marketplace and `RedeemOffer`/`redeemOfferDomain` for Redeem), not a separate `nft-order.ts` file.
services/sync/             The Clean Workflow's implementation — poller.ts (`runSync()`, chunked `getLogs` per deployed contract via `lib/onchain/sync-contracts.ts`'s `SyncTarget[]`, each target carries its own `eventName → handler` map since e.g. the two RwaEscrow deployments emit identically-named events but write to different tables), handlers.ts (`marketplaceEventHandlers`, `redeemNftEventHandlers`, `redeemRwaEventHandlers`), sync-state.ts (per-contract last-processed-block in `sync_state`).
store/                     Zustand + `persist` (→ localStorage). Only `mock-rebate.ts` and `mock-creator-fee.ts` are genuine mocks now (Rebate/Launchpad, out of scope) — Marketplace (NFT + RWA) and Redeem no longer use mock stores; `mock-redemptions.ts`/`mock-settings.ts` are dead empty stub files (kept only because this environment can't delete files — safe to remove).
lib/                       wagmi.ts, ipfs.ts, nft-collections.ts, nft-metadata-cache.ts, explorer.ts, toast.ts, utils.ts, image.ts, amount.ts (`parseBaseUnitsAmount` — validate base-units strings via BigInt, never `Number()`), redeem-format.ts, redeem-shipping-storage.ts, eip712.ts (real — see services/marketplace/ above), supabase/client.ts + supabase/server.ts (browser anon-key vs service-role clients), auth/session.ts (SIWE nonce cookies), onchain/public-client.ts + onchain/sync-contracts.ts + onchain/roles.ts, abis/ (erc721, erc20, nft-marketplace, rwa-escrow, permission-registry, juno-pts, redeem-nft-settlement, creator-fee-distributor — all real). No `mock/` directory anymore for Marketplace/Redeem.
types/                     marketplace.ts (NftListing, camelCase), rwa.ts (RwaListing/RwaStatus, camelCase — includes ship/dispute/auto-release/extension deadline constants mirrored from RwaEscrow.sol for UX countdowns only), redeem.ts (RedeemItem/RedemptionOrder/RedemptionStatus, **snake_case** matching the DB row shape directly, no camelCase mapping layer — a deliberately different convention from marketplace.ts/rwa.ts since Redeem was built after the Supabase schema was real), nft.ts, supabase.ts (hand-written `Database` type — keep `Views`/`Relationships` entries structurally complete or supabase-js's generics silently collapse table rows to `never`, see the git history on this file for the exact failure mode), plus kyc.ts/rebate.ts/analytics.ts/creator-fee.ts/web3.ts for adjacent features.
contracts/                 Foundry Solidity project, tracked directly in this repo (**not** a git submodule) — NftMarketplace.sol (gasless EIP-712 signed orders), RwaEscrow.sol (custodial escrow, fund→ship→confirm + refund/dispute + buyer-triggered one-time 7-day auto-release extension; deployed **twice** — once for Marketplace P2P trading, once dedicated to Redeem merch with a different feeBps/feeCollector), RedeemNftSettlement.sol (EIP-712 signed RedeemOffer, up to 3 price legs incl. JunoPts, Official-vs-Registered fee split), JunoPts.sol (KAP-22-style loyalty points ERC20 — a transfer needs at least ONE side, sender or recipient, to hold `AUTHORIZE_ROLE` via PermissionRegistry, so a holder can always pay a Registered merchant/escrow but two unregistered wallets can never trade points; `TRANSFER_ROUTER_ROLE` bypasses it entirely and belongs to contracts only), PermissionRegistry.sol (sole source of truth for Admin/PartnerMarketplace/PartnerRedeem/Authorize roles — never DB-stored), CreatorFeeDistributor.sol (Launchpad). Deploy scripts exist under `contracts/script/` for all of the above — check `.env.local`/`.env.example` for which contracts actually have a deployed address on the target chain; a missing `NEXT_PUBLIC_*_ADDRESS` means that one isn't deployed there yet.
supabase/migrations/       0001_base_schema.sql creates the real base tables (`users`, `nft_orders`, `rwa_listings`, `rwa_orders`); 0002-0007 add audit_logs, admin role + analytics views, nft_metadata_cache, applications, collections, sync_state; 0008_redeem_schema.sql adds the full Redeem schema (`redeem_items`, `redeem_item_variants`, `redemption_orders`, plus Google/Telegram/lister-branding columns on `users` and the `redeem_lister_profiles` public view). The schema is real and applies cleanly end to end.
docs/                      `Marketplace_Redeem_Feature.md` — current-state reference doc for Marketplace + Redeem (gitignored via `/Docs` in `.gitignore`, Windows filesystem makes `docs/`/`Docs/` the same dir — not committed, keep it updated by hand).

## Key conventions

- Runtime: **bun only** — never use npm, yarn, or pnpm.
- Language: **100% English everywhere in the product** — all UI text, labels, buttons, dialogs, toasts, empty states, error messages, and code comments must be in English. No Thai (or any other language) in user-facing strings, ever.
- Tests: test business logic, not framework behavior — skip tautologies, passthroughs, exact duplicates, and trivial defaults. `services/marketplace/*` is the main test target (EIP-712 payload correctness, fee calc, state-machine guard rejecting invalid transitions).
- Comments: comment only genuinely complex or non-obvious code — the *why*, gotchas, workarounds, magic-value/address decoders, sign conventions, contract-ABI correspondences, and math derivations. Do NOT add section-divider banners, one-word grouping labels, JSDoc that restates the function/type name, or inline narration of self-evident code.

## Clean workflow — the rule that governs this whole repo

**Implemented, not aspirational** — this is the real, live pattern every write hook in `hooks/` follows today (see `services/sync/` above for the poller itself). Follow it for any new write path.

Order/escrow status is **never** trusted from a client write or asserted client-side after a tx confirms. The lightweight on-chain sync poller (custom `getLogs` polling, not an indexer framework) is the only writer of status columns in Supabase. After any `writeContract` call:

1. Wait for tx confirmation.
2. Call `useSyncRefresh()` (`POST /api/sync/refresh`) to nudge the poller.
3. Invalidate the relevant React Query keys and re-fetch from Supabase.
4. If status hasn't updated yet (poller lag), poll again 2-3 times a few seconds apart with a "confirming on-chain" loading state — never flip local UI to "success" from the tx receipt alone.

Client-side checks in `services/marketplace/*-order.ts` (e.g. disabling a button that would obviously revert) exist for UX only. The contract's `require`/modifier is the only real source of truth — never relax a contract check because "the frontend already validated it."

## Forked from junoswap

`components/ui/*` is copied verbatim. `components/web3/*` (connect-button, connect-modal, jazzicon, network-switcher) is copied verbatim except `account-dropdown.tsx`, which has Multi-Send/Send stripped (out of scope here). `lib/wagmi.ts` keeps junoswap's `createConfig`/`cookieStorage`/`chainMetadata` shape but only lists `bitkub` + `kubTestnet` — every other chain junoswap supports is deliberately absent. Font is next/font Inter rather than junoswap's exact font setup; this is an intentional, accepted visual deviation.

## Notes

- **kub mainnet/testnet RPC** (`rpc.bitkubchain.io`) is NOT a full archive node. Historical `eth_call` reads fail with "missing trie node".
