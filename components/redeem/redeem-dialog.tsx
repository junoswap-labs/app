// Removed — redeeming now navigates to the item detail page (app/app/redeem/[itemId]/page.tsx)
// instead of opening a dialog. Left as an empty stub rather than deleted: file deletion is blocked
// in this environment.
export {}
