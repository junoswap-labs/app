// Removed — Redeem now uses real Supabase data (hooks/useRedeemItems.ts) instead of this mock
// catalog. Left as an empty stub rather than deleted: file deletion is blocked in this environment.
export {}
